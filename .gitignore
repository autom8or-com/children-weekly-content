# Secrets — never commit
.env

# Checkpoint — tracks generation progress per Sunday, safe to delete to restart fresh
.claude/checkpoint.json

# Temp spec files written during generation
/tmp/tod_specs/

# Node dependencies
node_modules/

# OS
.DS_Store
